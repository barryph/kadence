name: Frontend Tests

on:
  push:
    branches: [main]
    paths:
      - 'front-end/**'
      - '.github/workflows/frontend-test.yml'
  pull_request:
    paths:
      - 'front-end/**'
      - '.github/workflows/frontend-test.yml'

defaults:
  run:
    working-directory: front-end

jobs:
  jest:
    name: Jest (unit + integration)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: front-end/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Lint
        run: npm run lint

  # Maestro E2E requires an emulator, dev build, and running backend.
  # Uncomment and configure when CI infrastructure is ready.
  #
  # maestro-smoke:
  #   name: Maestro smoke
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #     - name: Start backend
  #       working-directory: back-end
  #       run: |
  #         npm ci
  #         npm run db:up
  #         npm run db:seed
  #         npm run start:dev &
  #     - name: Install Maestro
  #       run: |
  #         curl -Ls "https://get.maestro.mobile.dev" | bash
  #         echo "$HOME/.maestro/bin" >> $GITHUB_PATH
  #     - name: Run smoke flow
  #       env:
  #         TEST_EMAIL: ${{ secrets.E2E_TEST_EMAIL }}
  #         TEST_PASSWORD: ${{ secrets.E2E_TEST_PASSWORD }}
  #       run: npm run test:maestro:smoke
